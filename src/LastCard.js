// import React from 'react';
// import Card from './Card';

// const lastCard = ({ robots, theme, newCard, setRobots,newRobots, setNewRobots, id, newName, newEmail, newWebsite }) =>{
//     // let cardComponent = [];
//     // let lastCardComponent = [];
//     //  if (robots.lenght !== 0){
//     //      cardComponent = robots[robots.lenght-1];
    
//     // lastCardComponent = cardComponent.map((user, i) => {
//     //     return <Card theme={theme} key={i} id={newCard[i].id} newName={newCard[i].newName} newEmail={newCard[i].newEmail} newWebsite={newCard[i].newWebsite}/> 
//     //  })
//     // }
//     // //}
// //    console.log(newCard);
// //     // const cardComponent = newCard.map((robot, i) => {
// // 	// 	 return <Card theme={theme} key={i} id={newCard[i].id} newName={newCard[i].newName} newEmail={newCard[i].newEmail} newWebsite={newCard[i].newWebsite}/> 
// // 	// })
// // 	// console.log(newCard);

// // 	return(
// // 	<div >
// // 		{lastCardComponent}
// //     </div>

// // 	);

// }

// export default lastCard;